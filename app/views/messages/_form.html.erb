<%= simple_form_for([ game, message ], remote: true) do |f| %>
  <%= f.input :content, label: false,  as: :text %>
  <% if player.is_alive %>
    <%= f.button :submit, "Send", class: "btn btn-warning" %>
  <% end %>
<% end %>
